import Navbar from "./Navbar.jsx";

export default function Header() {
  return (
    <header className="bg-gradient-to-r from-[var(--ink)] to-[var(--nova-dark)] text-white shadow-md">
      <Navbar />
    </header>
  );
}
