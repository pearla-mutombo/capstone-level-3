import ReactDOM from "react-dom/client";

import App from "./App";
import "./index.css";
import "./App.css";

ReactDOM.createRoot(document.getElementById("root")).render(<App />);

// Note: My main.jsx is the entry point for my ReactDOM application. It finds the
// root element in my index.html and renders my App component.
