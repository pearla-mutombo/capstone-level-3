"use strict";
var import_chunk_GXDHVUPK = require("../../chunk-GXDHVUPK.js");
var import_chunk_OQQ4P4PG = require("../../chunk-OQQ4P4PG.js");
test("maskSchema", () => {
  const schema = `datasource db {
    url = "mysql:secret-db"
  }`;
  expect((0, import_chunk_GXDHVUPK.maskSchema)(schema)).toMatchInlineSnapshot(`
    "datasource db {
        url = "***"
      }"
  `);
  const schema2 = `datasource db {
    provider = "mysql"
    url = env("SOME_ENV")
  }`;
  expect((0, import_chunk_GXDHVUPK.maskSchema)(schema2)).toMatchInlineSnapshot(`
    "datasource db {
        provider = "mysql"
        url = "***"
      }"
  `);
});
